import React, { useContext, useReducer, useState } from "react";

// import context from '../../context';
import "./index.css";
import Card from "./Card";
import { useEffect } from "react";

const index = (proms) => {
  // let {pins} =proms

  // const [baxaaka, setUsers] = useState([]);

  // async function data() {
  //   const response = await fetch("https://api.github.com/users/baxaaka");
  //   const result = await response.json();
  //   console.log(result);
  // }

  // useEffect(() => {
  //   data();
  //   baxaaka
  // }, []);

  // return (
  //   <>
  //     <section className="section-5">
  //       <h1>over</h1>

  //       <div className="card">
  //         {/* {
  //                 pins.map((pin ,index)=>{
  //                   let{urls}=pin;
  //                    return  <Card key={index} urls ={urls}/>
  //                 })
  //               } */}
  //         {/* {
  //                 result.map((item)=>{
  //                   return ( <Card key={item.id} result={item} /> )
  //                 })

  //               } */}


  //         <div>
  //           {baxaaka.length > 0 && (
  //             <ul>
  //               {baxaaka.map((user) => (
  //                 <li key={user.id}> <h1>{user.login}</h1> {user.id}
  //                 <img src={user.avatar_url} alt="" /></li>
  //               ))}
  //             </ul>
  //           )}
  //         </div>
  //       </div>
  //     </section>
  //   </>
 

  return (
  //   <div>
  //     {baxaaka.length > 0 && (
  //       <ul>
  //         {baxaaka.map(user => (
  //           <li key={user.id}> <h1>{user.login}</h1> <h1>{user.html_url}</h1></li>
  //         ))}
  //       </ul>
  //     )}
  //   </div>
  
  // )
  <>

  </> )
}
;

export default index;
